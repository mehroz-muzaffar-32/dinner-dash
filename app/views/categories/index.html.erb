<h2>Browse Items by Categories</h2>
<% if policy(@categories).new? %>
  <%= link_to 'Add Category', new_category_path, class: 'btn btn-success mt-3' %>
<% end %>
<div class="list-group">
  <%= render @categories %>
</div>
